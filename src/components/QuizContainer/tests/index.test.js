import React from 'react';
import { render } from '@testing-library/react';
import { round } from './data';

import QuizContainer from '..';

const renderComponent = () => render(<QuizContainer />);

describe('<QuizContainer />', () => {
  it('', () => {});
});
